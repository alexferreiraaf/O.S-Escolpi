
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite que qualquer pessoa (autenticada ou não) leia e escreva
    // na coleção service_orders.
    match /service_orders/{document=**} {
      allow read, write: if true;
    }
  }
}
