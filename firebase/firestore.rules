rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to the service_orders collection
    match /service_orders/{document=**} {
      allow read, write: if true;
    }
  }
}
